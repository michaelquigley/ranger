# 2026-07-21 — daemon design session

Design session with Michael on the `desktop-integration` roadmap item, which grew past its original question ("tray + dfw windows against specific roots?") into the multi-project shape: ranger as a tray-resident daemon over every configured root, with a project selector in the UI. The spec landed at `docs/future/daemon.md`.

The settled calls, all made in conversation: explicit hand-edited config at `~/.config/ranger/config.yaml` (no discovery, no registration gestures); path-scoped API (`/api/v1/projects/{name}/…`, names on the wire, roots config-private — flo's source-path-privacy move); the daemon as the normal case with `ranger serve` surviving as the ad-hoc single-project degenerate case of the same server; per-project degradation under the daemon (a bad root flags in the selector, never kills the process) with fail-fast reserved for the daemon's own bootstrap and for ad-hoc serve; tray menu of open-board + quit, browser windows via URL, multiple windows free and uncoordinated. `dfw/tray` only — Michael explicitly chose browser windows over webview windows, keeping the CGO/webview dependency out.

Provisional in the spec, awaiting Michael's reaction: the command name (`ranger daemon`), the URL shape (`/p/{name}`), the projects-index endpoint shape, and the config field names. Scope boundary recorded: this is switching, not aggregation — `cross-repo-aggregation` stays on horizon, and the selector world is additive toward it.

Next: work order grounded in the code, then mercurius over spec + work order together. Worth remembering for the work order: the asset route must gain the project segment (`/roadmap/{name}/…`), the modal's URL transform needs the current project, and the spec change regenerates both sides of the ogen contract.

Spec approved by Michael on read. Work order drafted at `docs/future/daemon-work-order.md`: five stages — (1) config + project set, (2) the breaking contract restructure with server, assets, serve, and SPA mechanics moving together, (3) the selector, (4) the dfw tray daemon, (5) docs close-out. The one UI seam worth naming: `api.ts` becomes a project-bound client factory so call sites don't thread the project name. `mercurius.yaml` calibrated for the arc: review_focus names the two artifacts and the four scrutiny targets (contract completeness, error tiers, seam census calls, stage greenness); settled_decisions carries the re-earned corner-case-defensiveness guard (v1 + the 2026-07-20 race veto) and the spec's recorded deferrals; review_context gains the convention-primacy framing. Awaiting Michael's review of work order + calibration, then mercurius.

## the mercurius arc, converged

Six rounds (session `s_1YliYZO9qRJS`, synopsis at `.mercurius/s_1YliYZO9qRJS/_synopsis.md`): 18 findings, all fixed, none rejected, converging cleanly — 6+1 majors/advisories, then 3+2, 1+1, 1, 2, then round six **ready_to_build** with zero findings. The reviewer's catches were consistently grounded: the live-config-vs-startup-set contradiction, absolute paths leaking through diagnostics (settled as "names address, paths inform"), the fatal-panel selector trap, project-name URL grammar (settled with the convention's own slug rule), serve bypassing that same grammar, the capture-without-preflight wrong-tree write, and the transform-normalization containment hole (settled as a resolved-path prefix check, post-browser-normalization). Michael sharpened one call mid-review: `SpawnWindow` stays unset — no webview, ever — with one lowercase `open board` TrayItem.

Post-close self-audit over both artifacts end-to-end (the drift pass mercurius can't do) caught: the project-index response under-described in the spec (missing `default`, which the `/` redirect consumes); "name is the basename" surviving pre-slug-rule in the config section; `{name}`/`{project}` placeholder drift between the artifacts (harmonized to `{project}` for API and asset routes; `/p/{name}` stays the SPA shape in both); a stale "21 UI tests" count; and the critical-files table missing the modal props change and the tray icon asset. Michael blessed both provisional calls — `ranger daemon` and `/p/{name}` — and the markers are stripped; the spec header now records the converged-through-review provenance. Both artifacts are handoff-ready. Remaining before implementation begins: Michael's implementation-agent selection (Codex default for plan-following; Claude Code if stage 2's contract restructure wants creative latitude), and the working tree still carries the day's un-committed artifacts.
