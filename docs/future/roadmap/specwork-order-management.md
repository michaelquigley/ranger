---
title: spec+work-order management
state: researching
created: 2026-07-20
tags: [epic, spike]
milestone: v0.1.x
---

ranger is managing the `docs/future/roadmap/` folder as a planning and operational horizon. what if we were to make the relationship with spec+work-order artifacts more explicit in the ui, so that we had a better visualization of the work process?

## Background

carried over from the `additional-schema-structures` close-out (2026-07-29): pointers to spec and work-order artifacts are frontmatter-shaped, so this is the card that carries a genuine schema change — a claimed key, its malformed semantics, a span for surgical patch, a wire field, a render. that session settled the opposite direction for prose (supporting material is body sections, conventional and never validated); the split it drew is that the machine spine takes pointers and the body takes prose.
